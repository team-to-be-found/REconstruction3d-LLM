# Claude Engineering Galaxy · 设计规范

> **美学宪法**: This is not a graph visualization. It is a living engineering galaxy representing a Claude-centric cognitive system.

---

## 🎯 设计理念

**情绪基调**: Quiet. Intelligent. Confident. Not flashy. Not gamer. Not marketing.

**核心原则**:
- 这是**工程宇宙**，不是 Web3 官网
- 有**清晰的层级**，不是点云
- 有**真正的轨道**，不是随机散布
- **按需显示**，不是全部堆叠

---

## 🌌 视觉隐喻：行星系统

| 元素 | 隐喻 | 视觉特征 |
|------|------|---------|
| **中心** | Claude 核心机器人 | AI Brain / Agent Core |
| **第一轨道** | 核心工程能力 | Tooling / Prompt / Infra / Review |
| **第二轨道** | 可复用技能模块 | Skills |
| **第三轨道** | 项目级/实例级节点 | Projects / Instances |
| **最外层** | 冷数据 | Archived / Experimental |

---

## 🧠 中心设计：Claude Core

### 视觉规范

```
形态: 低多边形 / 半透明几何体
颜色: #E6F1FF + #7AA2FF + 微弱 cyan
大小: 整个画面权重的 18-22%
效果: 轻微呼吸光效（不是发光球）
```

### 交互行为

#### 鼠标靠近 (Hover)
- 眼睛亮起（如果有）
- 周围轨道减速
- 轻微放大 (scale 1.05)

#### 点击 (Click)
- 展开 Claude Profile Panel
- 显示信息：
  - Agent Version
  - Enabled Skill Count
  - 最近活跃模块
  - 工程成熟度（如：Engineering Readiness 82%）

---

## 🪐 轨道系统

### 轨道参数

| 轨道 | 半径范围 | 最大节点数 | 旋转速度 | Blur/Opacity |
|------|----------|-----------|---------|--------------|
| **Orbit 1** (Core Skills) | 15-20 | 8-12 | 慢速 (0.1) | 清晰 (1.0) |
| **Orbit 2** (Operational) | 25-35 | 12-24 | 中速 (0.3) | 中等 (0.9) |
| **Orbit 3** (Project/Edge) | 40-55 | 无限制 | 快速 (0.5) | 轻微模糊 (0.7) |

### 轨道行为约束

每个轨道有：
- ✅ 固定半径区间
- ✅ 最大节点数量
- ✅ 不同旋转速度
- ✅ 不同 blur / opacity

---

## 🎯 节点视觉规范

### 节点形态（信息晶体）

| 层级 | 形态 | 尺寸 | 用途 |
|------|------|------|------|
| **Core Skill** | 六边形 / 圆角菱形 | 大 (scale 3.0) | 核心能力 |
| **Skill** | 圆 / Soft capsule | 中 (scale 2.0) | 技能模块 |
| **Item** | 点 / 小晶体 | 小 (scale 1.0) | 具体项目 |

### 语义颜色系统

```typescript
// 禁止随机颜色，必须语义化
const SEMANTIC_COLORS = {
  // Prompt / LLM
  llm: {
    primary: '#5B8EFF',      // Blue
    secondary: '#4A5FC1',    // Indigo
    glow: '#7AA2FF'
  },

  // Infra / Tooling
  infra: {
    primary: '#00FFFF',      // Cyan
    secondary: '#00BFA5',    // Teal
    glow: '#4DD0E1'
  },

  // Review / QA
  review: {
    primary: '#FFB74D',      // Amber
    secondary: '#FFA726',
    glow: '#FFCC80'
  },

  // Automation
  automation: {
    primary: '#AB47BC',      // Purple
    secondary: '#8E24AA',
    glow: '#CE93D8'
  },

  // Experimental
  experimental: {
    primary: '#78909C',      // Gray
    secondary: '#546E7A',
    glow: '#90A4AE',
    opacity: 0.6             // 低透明度
  }
};
```

### 节点材质规范

```typescript
// 禁止纯色球体
material: {
  type: 'MeshStandardMaterial',  // 不是 MeshBasicMaterial
  roughness: 0.3,                 // 轻微反射
  metalness: 0.1,                 // 非金属感
  transparent: true,
  opacity: 0.85,                  // 半透明
  emissive: color,                // 自发光（弱）
  emissiveIntensity: 0.2          // 克制的 glow
}
```

---

## 🔗 连接线系统

### 默认状态
```
显示: 无（完全隐藏）
```

### Hover 某节点时
```
显示范围:
  - 与 Claude Core 的关系
  - 与同轨道最近 3 个节点

连接线样式:
  - 曲线（不是直线）
  - 透明度 < 30%
  - 轻微流动方向（data flow）
  - 颜色继承节点颜色
```

### 禁止事项
- ❌ 全连接
- ❌ 实线满屏
- ❌ 永远可见
- ❌ 蜘蛛网效果

---

## 🎮 交互层级

### Hover（悬停）
```
节点效果:
  - 放大 1.15x
  - 显示简短 Label（最多 2 行）
  - 其他节点 dim (opacity 0.3)
  - 显示相关连接线
```

### Click（点击）
```
场景效果:
  - Galaxy 暂停旋转
  - 其他节点进一步 dim

面板显示:
  - 右侧滑出 Node Detail Panel
  - 内容：
    * Name
    * Type
    * Description
    * File Path / Repo
    * Enabled / Disabled
    * Related Skills
```

### Drag（拖拽）
```
行为:
  - 允许轻微扰动
  - 松手后回到轨道（弹性动画）
  - 不允许拖出轨道范围
```

---

## 🌑 背景与空间感

### Background Gradient
```
颜色:
  - 不是纯黑 (#000000)
  - 深蓝黑渐变
    top: #0A0E27
    bottom: #1A1F3A

纹理:
  - 微弱噪声纹理（不是星空 PNG）
  - 噪声强度: 5-10%
```

### 空间层次（Parallax）
```
至少 3 层:
  - Layer 1 (前景): 中心机器人 + 第一轨道
  - Layer 2 (中景): 第二、三轨道
  - Layer 3 (远景): 极少粒子（避免廉价）

景深效果:
  - 中心最清晰
  - 外围逐渐 blur + fade
```

---

## 🛠️ 技术实现指南

### 推荐技术栈
```
✅ Three.js + InstancedMesh
✅ 或 WebGPU（如果追求极致性能）
❌ 禁止 D3 默认力导向（会毁一切）
```

### 物理系统
```
约束类型: 自定义 orbital constraints
物理引擎: 非真实物理（不是重力模拟）
碰撞检测: 仅轨道内
```

### 性能优化
```
- 使用 InstancedMesh 批量渲染相同形状
- LOD (Level of Detail) 系统
- Frustum Culling（视锥剔除）
- 连接线按需生成，不预加载
```

---

## 📐 尺寸规格表

### 画布尺寸
```
推荐: 1920x1080 (16:9)
最小支持: 1280x720
```

### 节点尺寸
```
Center Robot:  18-22% 画面权重
Core Skill:    3.0 units
Skill:         2.0 units
Item:          1.0 units
```

### 轨道半径
```
Orbit 1:  15-20 units
Orbit 2:  25-35 units
Orbit 3:  40-55 units
```

---

## 🎨 动画曲线

### 缓动函数
```
Hover:      easeOutCubic
Click:      easeInOutQuad
Drag:       spring (damping: 0.5)
Rotate:     linear (constant speed)
```

### 动画时长
```
Hover:      200ms
Click:      300ms
Panel:      400ms
Orbit:      60-120s (一圈)
```

---

## ✅ 验收标准

### 视觉检查清单
- [ ] 中心机器人清晰可见，有呼吸效果
- [ ] 轨道层次分明，不是点云
- [ ] 颜色语义化，不是荧光绿
- [ ] 节点有大小层级，不是全部一样
- [ ] 连接线默认隐藏，hover 时才显示
- [ ] 背景深邃，有渐变和噪声

### 交互检查清单
- [ ] Hover 节点有放大和 dim 效果
- [ ] Click 节点显示详情面板
- [ ] Drag 节点有弹性回归
- [ ] 轨道旋转平滑，点击时暂停
- [ ] 整体流畅，60fps

### 性能检查清单
- [ ] 100+ 节点时 FPS ≥ 60
- [ ] 内存稳定，无泄漏
- [ ] 首次加载 < 2 秒

---

## 🚫 禁止事项总结

### 视觉禁止
- ❌ 荧光绿大圆
- ❌ 纯色球体
- ❌ 随机颜色
- ❌ 同权重节点
- ❌ 星空 PNG 背景

### 布局禁止
- ❌ 无轨道的点云
- ❌ 全连接蜘蛛网
- ❌ D3 力导向布局
- ❌ 随机散布

### 交互禁止
- ❌ 无反馈的点击
- ❌ 永远显示的连接线
- ❌ 拖拽无约束
- ❌ 旋转速度不可控

---

**文档版本**: v1.0
**创建时间**: 2026-01-30
**状态**: 实施中

---

**下一步**: 参考 `IMPLEMENTATION_PLAN.md` 开始分阶段实施
