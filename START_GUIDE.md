# 🚀 3D 知识图谱 - 启动指南

## 快速启动（推荐）

### 方法 1：一键启动脚本

**Windows 用户**：
```bash
# 双击运行
start-dev.bat

# 或在命令行中
.\start-dev.bat
```

**Git Bash / Linux / Mac 用户**：
```bash
bash start-dev.sh
# 或
./start-dev.sh
```

### 方法 2：使用 npm 命令

```bash
# 标准启动
npm run dev

# 清理缓存后启动（推荐在出现问题时使用）
npm run dev:clean

# 使用启动脚本（自动清理端口）
npm run start:dev
```

---

## 访问地址

启动成功后，访问以下地址：

- **主页（原始UI）**: http://localhost:3000
- **V2 赛博朋克UI**: http://localhost:3000/v2

---

## 常见问题

### ❓ 端口被占用怎么办？

**方法 1**：使用自动清理脚本
```bash
# 运行启动脚本，它会自动清理端口
bash start-dev.sh
```

**方法 2**：手动清理端口
```bash
npm run kill-ports
```

**方法 3**：手动查找并结束进程
```bash
# 查找占用端口的进程
netstat -ano | findstr :3000

# 结束进程（PID 是上面命令显示的最后一列数字）
taskkill /PID <PID> /F
```

### ❓ 页面无法加载或出现错误？

1. **清理缓存重启**
   ```bash
   npm run dev:clean
   ```

2. **重新安装依赖**
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

3. **检查 Node.js 版本**
   ```bash
   node --version  # 应该 >= 18.0.0
   ```

### ❓ V2 页面无法访问？

确保访问的是 **http://localhost:3000/v2**（注意端口是 3000，不是 3001）

如果服务器运行在其他端口（如 3001），请访问对应端口的 /v2 路由。

---

## 开发环境要求

- **Node.js**: >= 18.0.0
- **npm**: >= 9.0.0
- **操作系统**: Windows / macOS / Linux
- **浏览器**: Chrome / Edge / Firefox（最新版）

---

## 项目结构

```
reconstruction-3d/
├── src/
│   ├── app/               # Next.js 应用目录
│   │   ├── page.tsx       # 主页（原始UI）
│   │   └── v2/
│   │       └── page.tsx   # V2 赛博朋克UI
│   ├── components/
│   │   ├── scene/         # 3D 场景组件
│   │   ├── ui/            # 原始 UI 组件
│   │   └── ui-v2/         # V2 赛博朋克 UI 组件
│   └── stores/            # Zustand 状态管理
├── start-dev.sh           # 启动脚本（Git Bash）
├── start-dev.bat          # 启动脚本（Windows）
└── package.json
```

---

## 技术栈

- **框架**: Next.js 14.2
- **3D 渲染**: Three.js + React Three Fiber
- **状态管理**: Zustand
- **样式**: Tailwind CSS
- **UI 库**: Radix UI + Lucide Icons

---

## 下一步

1. 访问 http://localhost:3000/v2 查看赛博朋克风格 UI
2. 点击节点查看详情面板
3. 使用左侧栏过滤节点类型
4. 使用顶部搜索栏搜索节点

---

## 需要帮助？

遇到问题请检查：
1. Node.js 版本是否 >= 18
2. 端口 3000 是否被占用
3. .next 缓存是否需要清理
4. 依赖是否正确安装

**重启大法**：
```bash
npm run kill-ports
npm run clean
npm install
npm run dev
```
